{
  "name": "@acme/ui",
  "version": "1.0.0",
  "private": true,
  "exports": {
    "./*": "./dist/*.js"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "lint": "eslint .",
    "build": "rimraf dist && tsc",
    "postinstall": "npm run build",
    "dev": "tsc --watch"
  },
  "peerDependencies": {
    "react": "19.2.0",
    "react-native": "0.81.5"
  },
  "devDependencies": {
    "@acme/eslint-config": "*",
    "@acme/typescript-config": "*",
    "rimraf": "3.0.2"
  }
}
